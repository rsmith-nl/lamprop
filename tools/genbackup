# Shell script to generate a full backup.
# -*- shell-script -*-
NAME=py-lamprop
NUM=$(git log -n 1 --oneline |cut -c 1-7)
DF=${NAME}-${NUM}.tar.gz
cd .. ; tar czf /tmp/${DF} ${NAME}/
mv /tmp/${DF} ${NAME}/
